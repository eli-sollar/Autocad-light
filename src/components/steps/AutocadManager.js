import httpRequest from "../../utility/httpRequest";

export default class AutocadManager{
    
    async saveAutocadData(parameters) {
        
      }
      
      loadAutocadData(result,projectId) {
     
    }
}